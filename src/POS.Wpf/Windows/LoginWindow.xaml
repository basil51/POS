<Window x:Class="POS.Wpf.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="POS — Sign in"
        Height="260"
        Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Username" Margin="0,0,0,4" />
        <TextBox Grid.Row="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />
        <TextBlock Grid.Row="2" Text="Password" Margin="0,0,0,4" />
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Margin="0,0,0,16" PasswordChanged="PasswordBox_OnPasswordChanged" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Sign in" Width="100" IsDefault="True" Command="{Binding LoginCommand}" />
        </StackPanel>
    </Grid>
</Window>
